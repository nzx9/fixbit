language: node_js
node_js:
  - "stable"

branches:
  only:
    - main

cache:
  directories:
    - node_modules

install:
  - yarn install

deploy:
  provider: heroku
  cleanup: false
  keep_history: true
  api_key:
    secure: $HEROKU_API_KEY
  app: fixbit-app
